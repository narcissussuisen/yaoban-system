# QUEUE_SAMPLE_2026-09-15 · 形态门启用首日「确认队列」实盘样本

> **性质**：只读采集与报告。**未改任何参数、未改任何代码。**
> **采集人**：小wb（自动化任务）
> **采集时间**：2026-09-15 15:20+（收盘后）
> **样本窗口**：09:30:07 → 15:00:07，共 **236 轮**（`outputs/intraday/confirm_20260915_*.json`）
> **运行模式**：`scan_and_confirm.py --pattern-gate`，`mode_flags = {e4_support:true, temp_ladder:true, execute:true, pattern_gate:true}`，`min_amt_yi=10.0`

---

## 〇、样本口径声明（⚠️ 先读这条）

**本样本由 commit `e59b0a1` 之前的代码产出。**

| 项 | 值 |
|---|---|
| 样本运行时间 | 2026-09-15 09:30:07 – 15:00:07（236 轮） |
| `scan_and_confirm.py` 最后一次变更 | **`e59b0a1` @ 2026-09-15 15:22**（晚于全部样本轮次） |
| `e59b0a1` 内容 | `fix(selection): R4 成交额口径改回「T−1 全天」+ lookback 残留清理（备选池零命中 P0）` |

⇒ 今日 236 轮跑的都是 **R4 修复前** 的口径（`rough_screen` 的成交额闸用**当日累计**而非 T−1 全天）。**明日的行为会与今日样本不同**，不可直接用今日样本推明日。

**两个连带结论（重要）**：

1. `e59b0a1` 新增了 `pattern_meta.funnel.rejects`（活跃闸逐判据拒绝计数）。**今日 236 轮全部没有这个字段**——今日"某票为何被挡"只能推断，这正是该字段要补的空白。
2. `e59b0a1` 的 R4 修复依赖池记录里的 `prev_amt_yi`，但**今日池产物 1246 条记录中 `prev_amt_yi` 字段数为 0**（字段清单仅 `sym/pattern/pattern_cn/patterns/sig_date/bars_since_sig/close_asof`）。
   ⇒ **若不重建战法池（`scripts/plan_daily.py`），新口径会退回当日累计**（代码内会记 `amt_legacy_basis`，不静默）。**这是明日开盘前必须确认的一件事。**

---

## 1. 漏斗：战法池 → 确认队列

> ⚠️ **口径更正**：任务书把 `len(movers)` 列为漏斗中段，**这不成立**。
> 按源码（`scan_and_confirm.py:409-414`），`movers = pool[:20]`，其中 `pool` = **全市场**过 `rough_screen` 者按涨幅降序；
> 且注释明写：「movers 自 2026-09-14 起只是**发现层/留痕**，**不参与选股**」。
> 实证：收盘轮 `confirm_queue` 15 只中只有 6 只在 `movers` 里；`movers` 20 只中有 9 只**不在战法池**。
> ⇒ **`movers` 与 `confirm_queue` 是两条独立产物，不是上下游。**
> 真正的漏斗在 `pattern_meta.funnel` 内。

### 1.1 战法池（`pattern_meta`，全 236 轮完全一致）

| 项 | 值 |
|---|---|
| `asof` | **2026-09-14**（T-1，严格早于 day） |
| `lookback` | 6 |
| `built_at` | 2026-09-15 06:03:14 |
| `n_syms_scanned` | 5644 |
| `n_skipped_short` / `n_excluded_st` / `n_no_name` / `n_excluded_fanbao_only` | 40 / 212 / 420 / 992 |
| **`n_pool`** | **1246** |
| `n_zt_watch_added` | 8 |

`stats.by_pattern`：`huigui` **1133**（90.9%）/ `zt_huicai` **97**（7.8%）/ `xianren` **8** / `zt_watch` **8**

**✅ 已验证**：本日 41 只队列成员**全部在池内**（`NOT in pool = 0`）⇒ 形态门准入确实严格受池约束。

### 1.2 真实漏斗（`pattern_meta.funnel`，逐轮）

`n_pool` → `n_pat_active` → `n_dropped_cold_l2` → `n_hot_active` → `n_queue_capped`

| 时刻 | n_pool | n_pat_active | n_dropped_cold_l2 | n_hot_active | n_strong_only | **n_queue_capped** | (对照) len(movers) |
|---|---|---|---|---|---|---|---|
| 09:30–09:32 | 1246 | 0 | 0 | 0 | 0 | **0** | 0 |
| 09:33 | 1246 | 1 | 0 | 1 | 0 | **1** | 1 |
| 09:46 | 1246 | 12 | 1 | 11 | 4 | **11** | 13 |
| 10:00 | 1246 | 18 | 1 | 17 | 8 | **15** | 20 |
| 11:30 | 1246 | 31 | — | 27 | 14 | **15** | 20 |
| 13:30 | 1246 | 36 | — | 31 | 19 | **15** | 20 |
| **15:00** | 1246 | **45** | 8 | **37** | 18 | **15** | 20 |
| **全天峰** | 1246 | **46** | 8 | **39** | **22** | **15** | **20** |

**漏斗三档数字（收盘轮）**：`n_pool = 1246` → 活跃闸通过 `n_pat_active = 45` → 板块闸后 `n_hot_active = 37` → **队列 `n_queue_capped = 15`**

**关键量化**：
- 活跃闸（`rough_screen`）把池内候选从峰值 **46 砍到 39** 量级（全天存活率约 85%）。
- 板块闸（非当日热点 ∧ 非 T-1 强主线即丢）再砍 8 只（`n_dropped_cold_l2` 收盘 8）。
- **`cap=15` 截断**：收盘 `n_hot_active=37` → `n_queue_capped=15` ⇒ **22 只（59.5%）被 cap 丢弃，且无留痕**（高峰时 39→15，丢 24 只 = 61.5%）。

> ⚠️ **`cap=15` 丢弃无留痕**是本样本暴露的第一个证据缺口：被截断的 22–24 只**无从复核**。

---

## 2. 队列规模分布

| 指标 | 值 |
|---|---|
| **min** | **0**（仅 09:30 / 09:31 / 09:32 三轮，盘前预热无行情） |
| **median** | **15.0** |
| **max** | **15** |
| mean | 14.29 |
| 落在 ROADMAP 目标 [5,15] | **229 / 236 = 97.0%** |
| **> 15 的轮次** | **0**（结构上不可能，有 cap） |
| **恰好 = 15 的轮次** | **213 / 236 = 90.3%** |

分布：`{0:3, 1:2, 3:1, 4:1, 5:2, 7:1, 9:1, 10:1, 11:6, 12:1, 13:2, 14:2, 15:213}`

> ⚠️ **规模指标不含信息**：90.3% 的轮次恰好顶在 `cap=15`。队列规模由 **cap 决定**，不由信号强弱决定。
> 「97% 落在 [5,15]」是 cap 的**算术结果**，**不能读作拟合良好**。
> 若要衡量真实信号量，应看 `n_hot_active`（收盘 37）——它是**队列容量的 2.5 倍**。

---

## 3. ⭐ 全天队列成员并集（41 只）

**本任务最重要的产物。** 41 只成员，按进入轮次数降序：

| sym | 名称 | pattern | sig_date | bars | 进入轮次 | in_plan | in_hot | L2 |
|---|---|---|---|---|---|---|---|---|
| 002436 | 兴森科技 | huigui | 2026-09-09 | 3 | **229** | ✗ | ✓ | 元件 |
| 002636 | 金安国纪 | huigui | 2026-09-14 | 0 | **226** | ✗ | ✓ | 元件 |
| 300903 | 科翔股份 | xianren | 2026-09-14 | 0 | **226** | ✗ | ✓ | 元件 |
| 603936 | 博敏电子 | zt_huicai | 2026-09-08 | 4 | **214** | ✗ | ✓ | 元件 |
| 002815 | 崇达技术 | huigui | 2026-09-10 | 2 | **212** | **✓** | ✓ | 元件 |
| 301366 | 一博科技 | huigui | 2026-09-11 | 1 | 178 | ✗ | ✓ | 元件 |
| 601208 | 东材科技 | huigui | 2026-09-07 | 5 | 178 | ✗ | ✓ | 塑料 |
| 603989 | 艾华集团 | huigui | 2026-09-09 | 3 | 174 | ✗ | ✓ | 元件 |
| 002579 | 中京电子 | huigui | 2026-09-10 | 2 | 168 | ✗ | ✓ | 元件 |
| 300806 | 斯迪克 | huigui | 2026-09-11 | 1 | 148 | ✗ | ✓ | 塑料 |
| 301132 | 满坤科技 | huigui | 2026-09-14 | 0 | 142 | ✗ | ✓ | 元件 |
| 600410 | 华胜天成 | zt_huicai | 2026-09-14 | 0 | 134 | ✗ | 混 | IT服务Ⅱ |
| **002886** | **沃特股份** | huigui | 2026-09-07 | 5 | 129 | ✗ | ✓ | 塑料 |
| 603236 | 移远通信 | huigui | 2026-09-14 | 0 | 120 | ✗ | ✗ | 通信设备 |
| 000823 | 超声电子 | huigui | 2026-09-09 | 3 | 95 | ✗ | ✓ | 元件 |
| 002560 | 通达股份 | zt_huicai | 2026-09-14 | 0 | 75 | ✗ | ✗ | 电网设备 |
| 603386 | 骏亚科技 | zt_huicai | 2026-09-14 | 0 | 63 | ✗ | ✓ | 元件 |
| 600184 | 光电股份 | zt_huicai | 2026-09-14 | 0 | 62 | ✗ | ✗ | 地面兵装Ⅱ |
| 002212 | 天融信 | zt_watch | 2026-09-14 | 0 | 59 | ✗ | 混 | 软件开发 |
| 000981 | 山子高科 | zt_watch | 2026-09-14 | 0 | 55 | ✗ | ✗ | 汽车零部件 |
| 300811 | 铂科新材 | huigui | 2026-09-08 | 4 | 44 | ✗ | ✓ | 金属新材料 |
| 002897 | 意华股份 | zt_watch | 2026-09-14 | 0 | 41 | ✗ | ✗ | 通信设备 |
| 603618 | 杭电股份 | huigui | 2026-09-11 | 1 | 41 | ✗ | ✗ | 电网设备 |
| 000980 | 众泰汽车 | huigui | 2026-09-11 | 1 | 37 | ✗ | ✗ | 汽车零部件 |
| 600792 | 云煤能源 | huigui | 2026-09-09 | 3 | 37 | ✗ | ✓ | 焦炭Ⅱ |
| 000636 | 风华高科 | zt_huicai | 2026-09-14 | 0 | 35 | ✗ | ✓ | 元件 |
| 002138 | 顺络电子 | zt_huicai | 2026-09-14 | 0 | 35 | ✗ | ✓ | 元件 |
| 600183 | 生益科技 | huigui | 2026-09-07 | 5 | 29 | ✗ | ✓ | 元件 |
| 000993 | 闽东电力 | zt_huicai | 2026-09-10 | 2 | 28 | ✗ | ✗ | 电力 |
| 002141 | 贤丰控股 | zt_watch | 2026-09-14 | 0 | 28 | ✗ | ✓ | 元件 |
| 301176 | 逸豪新材 | huigui | 2026-09-14 | 0 | 28 | ✗ | ✓ | 元件 |
| 001389 | 广合科技 | zt_huicai | 2026-09-08 | 4 | 27 | ✗ | ✓ | 元件 |
| 300454 | 深信服 | huigui | 2026-09-07 | 5 | 16 | ✗ | 混 | 软件开发 |
| 600601 | 方正科技 | huigui | 2026-09-09 | 3 | 16 | ✗ | ✓ | 元件 |
| 600172 | 黄河旋风 | huigui | 2026-09-07 | 5 | 14 | ✗ | ✗ | 通用设备 |
| 002015 | 协鑫能科 | huigui | 2026-09-10 | 2 | 9 | ✗ | ✓ | 电力 |
| 002531 | 天顺风能 | huigui | 2026-09-14 | 0 | 6 | ✗ | ✓ | 风电设备 |
| 605006 | 山东玻纤 | huigui | 2026-09-14 | 0 | 6 | ✗ | 混 | 玻璃玻纤 |
| 603115 | 海星股份 | huigui | 2026-09-09 | 3 | 4 | ✗ | ✓ | 电子化学品Ⅱ |
| 603938 | 三孚股份 | huigui | 2026-09-10 | 2 | 3 | ✗ | ✗ | 化学原料 |
| 300570 | 太辰光 | huigui | 2026-09-11 | 1 | 1 | ✗ | ✗ | 通信设备 |

### 3.1 构成统计

| 维度 | 分布 |
|---|---|
| **pattern** | `huigui` 27（65.9%）/ `zt_huicai` 9（22.0%）/ `zt_watch` 4（9.8%）/ `xianren` 1（2.4%） |
| **sig_date** | 09-14: 17 / 09-09: 6 / 09-10: 5 / 09-07: 5 / 09-11: 5 / 09-08: 3 |
| **L2 板块** | **元件 18（43.9%）** / 通信设备 3 / 塑料 3 / 软件开发 2 / 电力 2 / 汽车零部件 2 / 电网设备 2 / 其余 9 个行业各 1 |
| `in_hot=true`（曾） | 30 / 41 |
| `in_hot=false`（全程） | 11 / 41 |
| `strong_mainline_only=true`（曾） | 15 / 41 |
| **`in_plan=true`（曾）** | **1 / 41**（仅 002815 崇达技术） |

### 3.2 粘滞度（队列几乎不换人）

- 前 5 名占据 **229 / 226 / 226 / 214 / 212** 轮（全天 236 轮）
- 相邻轮次成员**对称差**：`{0: 133轮, 1: 8轮, 2: 81轮, 3: 1轮, 4: 12轮}` ⇒ **133/235 = 56.6% 的相邻轮次队列完全不变**
- 进出以 `sig_date` 到期为主因 ⇒ 该队列不是「每分钟重选」，而是**一次选定后钉住**

**为什么粘滞**（源码 `scan_and_confirm.py:475-484`，2026-09-14 裁定）：排序键 =
```
(not in_plan, not in_hot, bars_since_sig, -len(patterns), -chg)   → 再 [:15]
```
`in_plan` 是**第一主键**、`in_hot` 第二、`-chg` 仅作**末位 tiebreak**。所以队列与涨幅解耦（见 §4 验证），但也因此**极少重排**。

---

## 4. 触发与成交

| 指标 | 值 |
|---|---|
| **`total_triggered`（全天）** | **0** |
| `outcome` 分布（filled / request / rejected） | **空——全天无任何 outcome 事件** |
| `execution_rejected` 原因归类 | **无（0 条）** |
| `fill_ids` 明细（sym/qty/px/rule/时间） | **无（0 条）** |
| `request_ids` | **空** |

**读法**：236 轮 `triggered` 全为空。这**不是**执行段静默丢弃（§5 通过），**也不是**被拒绝——而是**没有任何标的在任何一轮满足触发条件**，执行链路全天未被激活。

---

## 5. 静默检查（`n_no_outcome`）

| 项 | 结果 |
|---|---|
| `n_no_outcome` 取值分布 | **`{0: 236}`** |
| 是否恒为 0 | ✅ **是** |
| 非零轮次 | **无** |
| 字段缺失轮次 | **无** |

✅ **执行段无静默丢弃。** 本日不存在「有触发但无 outcome」的缺陷形态。

---

## 6. 口径检查

| 检查项 | 结果 |
|---|---|
| `pattern_meta.asof` | **全部 236 轮 = `2026-09-14`**，严格早于 day(2026-09-15) ✅ |
| `pattern_meta` 缺失轮次 | **0** ✅ |
| `strong_mainline.date` | 2026-09-14（`sector_strength_2026-09-14.json`） |
| `strong_mainline.calibrated` | ⚠️ **`false`**（全 236 轮）——强势主线闸门**未标定**，却已放行 `n_strong_mainline_only` 峰值 **22 只** |
| **`rc=8` 轮次** | **0**（无 fail-closed） ✅ |
| 队列因战法池缺失而为空的轮次 | **0**（战法池全程就位） ✅ |
| 确认队列为空的轮次 | **3 轮：09:30 / 09:31 / 09:32** —— 盘前预热，`n_pat_active=0`、`movers=0`，**属正常预热，非战法池缺失** |
| `funnel.rejects` 字段 | ⚠️ **全 236 轮缺失** —— 由 `e59b0a1`(15:22) 新增，晚于本样本（见 §0） |

### 6.1 `task_logs\2026-09-15\*_scan.json` 的 exit_code 分布

| 项 | 值 |
|---|---|
| scan 日志数 | **326** |
| **exit_code = 0** | **325** |
| **exit_code = 4** | **1** |
| **exit_code = 8** | **0** |

**唯一非 0 明细**：

```
run_id      : run-20260915_093401_266
mode        : scan
exit_code   : 4
finished_at : 2026-09-15 09:34:01
stderr      : 行情覆盖不完整: quotes=4289/4689 coverage=91.5%
              failures=[{'offset': 0, 'error': 'URLError'}]
```

**判读**：`rc=4` 成因是 **`URLError` 致行情覆盖 91.5%（4289/4689）**，属**数据源/网络瞬时抖动**，**不是** `fail-closed`（rc=8）。该轮 `confirm_20260915_0933.json` 已正常产出（`len(queue)=1`），**未造成中断**。

同目录其他任务类型 `rc` 全为 0：`monitor` 328 / `notify` 345 / `auction` 16 / `tdx-probe` 13 / `close` 1 / `infra` 1 / `selfheal` 1 / `premarket` 1 / `plan-gate` 1 / `morning-check` 1。
全目录 **1034 个任务日志中仅此 1 条非 0**。**当日无 rc=8。**

---

## 7. 与选手候选池对照

✅ **选手 9/15 资料已到**（非缺失）。

**来源**：`选手学习资料\文字资料\2026-09-15_0945_候选池\record.md`（发布 09:45，战法依据 = 上升回档战法）

| 序 | 选手候选 | 代码 | 我方队列是否命中 | pattern / sig_date | 进入轮次 |
|---|---|---|---|---|---|
| 1 | 艾华集团 | 603989 | ✅ **命中** | huigui / 2026-09-09 | 174 |
| 2 | 协和电子 | 605258 | ❌ **未命中** | — | — |
| 3 | 沃特股份 | 002886 | ✅ **命中** | huigui / 2026-09-07 | 129 |
| 4 | 国脉科技 | 002093 | ❌ **未命中** | — | — |

### 命中率

| 口径 | 结果 |
|---|---|
| **命中率（交集 / 选手候选数）** | **2 / 4 = 50.0%** |
| 我方并集覆盖率（交集 / 41） | 2 / 41 = 4.9% |

### 按「选手实际执行」口径（更关键）

选手 9/15 **实际买入 2 只**（据 `2026-09-15_1048_持仓截图\record.md`）：

| 实际执行票 | 我方是否命中 |
|---|---|
| **沃特股份 002886** | ✅ 命中（进入 129 轮） |
| **国脉科技 002093** | ❌ **未命中（全天未进队列）** |

⇒ **选手实际执行票命中率 = 1 / 2 = 50%**。形态门**漏掉选手当日一半的实际买入**。

---

## 8. 与系统自身 day_plan 的对照

`outputs/plans/2026-09-15_plan.json`（published 2026-09-14 17:29，premarket 08:50 刷新），4 只 picks：

| 计划票 | 名称 | L2 | **是否在战法池** | **是否进过 confirm_queue** |
|---|---|---|---|---|
| 002815 | 崇达技术 | 270200 | ✅ 是 | ✅ **是**（212 轮，自 09:58 起） |
| 002913 | 奥士康 | 270200 | ✅ 是 | ❌ **否** |
| 000565 | 渝三峡Ａ | 220300 | ✅ 是 | ❌ **否** |
| 000690 | 宝新能源 | 410100 | ✅ 是 | ❌ **否** |

⇒ **4 只计划票全部在战法池内（结构上可见），但形态门队列全天只放行 1 只（1/4 = 25%）。**

**⚠️ 这不是 R1（窗口不匹配）造成的**：R1 说的是「计划层 4 日窗看不到池内 6 日窗的候选」；而此处 4 只计划票**都在池内**，被挡在后面的闸门。
被挡的闸只可能是 `rough_screen`（活跃闸）或 `in_hot/in_strong`（板块闸）——而**今日样本没有 `funnel.rejects` 落痕，无法逐只归因**。这正是 §0 提到的证据缺口。

### 8.1 附带发现：计划 `sig_date` 与战法池 `sig_date` 不一致

| 票 | plan 的 `sig_date` | 池的 `sig_date` | 池 `bars_since_sig` |
|---|---|---|---|
| 002815 崇达技术 | 2026-09-14 | **2026-09-10** | 2 |
| 002913 奥士康 | 2026-09-14 | **2026-09-10** | 2 |
| 000565 渝三峡Ａ | 2026-09-10 | 2026-09-10 | 2 |
| 000690 宝新能源 | 2026-09-09 | **2026-09-10** | 2 |

⇒ **4 只中 3 只的 `sig_date` 两处不一致**，与既有 RCA 的 R1（计划窗 4 日 vs 池窗 6 日）方向一致，但**现象独立**：同一个信号日在两个产物里被算成不同日期。建议一并复核。

---

## 9. 与既有 RCA 的关系（避免重复劳动）

`EvoAlpha\docs\RCA_PLAN_PICKS_MISS_20260915.md`（并行会话产出，触发于 2026-09-15 13:49）已对该问题做过根因分析，结论与本样本**互补**：

| RCA 编号 | 内容 | 状态 | 与本样本的关系 |
|---|---|---|---|
| **R1** | 计划信号窗(4日) 窄于池召回窗(6日) ⇒ 596 只池内候选对计划层不可见 | ✅ 已修 | 本样本未覆盖（本样本只观测队列侧） |
| **R2** | picks 排序键退化 + 不稳定排序 ⇒ ①并列者"任取 2 只" | ✅ 已修 | 同上 |
| **R3** | `in_plan` 是排序首键 ⇒ 计划 4 只锁定全链 | ⏳ 待裁定 | ✅ **本样本独立验证**：`in_plan` 确为排序第一键（源码 475-481），41 只成员中仅 002815 曾 `in_plan=true` |
| **R4** | `rough_screen` 成交额/换手是**当日累计值** ⇒ 早盘几乎全挡 | ⏳ 待裁定 | ✅ **本样本即"R4 修复前"的最后一份全日样本**（见 §0），可用作修复前的基线 |
| **R5** | L2 硬过滤（非当日热点 ∧ 非 T-1 强主线 ⇒ 直接丢） | ⏳ 待补证据 | ✅ 本样本给出量化：收盘 `n_dropped_cold_l2 = 8`；`strong_mainline_only` 峰值 22 |

**本样本相对 RCA 的增量证据**：
1. **队列侧全天 236 轮的完整样本**（RCA 主要看 09:46 单点）
2. **`cap=15` 截断损失量化**：收盘 37→15，**22 只（59.5%）无痕丢弃**
3. **队列粘滞度量化**：56.6% 相邻轮次零变化
4. **板块集中度量化**：元件 43.9%
5. **`prev_amt_yi` 缺失发现**（§0 第 2 条）——**R4 修复的生效前提未就位**
6. **4 只计划票全在池内但仍只放行 1 只** ⇒ 把问题从"可见性"推进到"闸门归因"

---

## 结论：形态门上线首日的实际效果

### ✅ 做对的部分

1. **确已切断「涨幅主导」。** 队列涨幅均值 **3.18%** / 中位 **2.65%**；`|chg|<5%` 占 **70.9%**；**负涨幅占 10.2%**。收盘队列里出现 **顺络电子 −0.86%、逸豪新材 −0.65%、广合科技 −0.31%** 这类标的——它们**绝不可能**出现在旧的「涨幅前 8」。**选股层与涨幅榜已解耦，核心目标达成。**（源码侧印证：`-chg` 已降为末位 tiebreak，`scan_and_confirm.py:480`）
2. **准入严格受战法池约束。** 41 只成员 **100% 在池内**。
3. **口径正确。** `asof` 恒为 T-1（2026-09-14），无未来函数；池全程就位（`n_pool=1246` 无一轮缺失）。
4. **无静默失效。** `n_no_outcome` 全 236 轮为 0；当日 **无 rc=8**；326 个 scan 日志 325 个 rc=0；唯一 rc=4 是 URLError 数据源抖动，未致中断。
5. **四类形态均有产出**（huigui 65.9% 主导，zt_huicai/zt_watch/xianren 共 34.1%），非单形态退化。

### ⚠️ 直说的问题（不粉饰）

1. **队列规模指标是假的。** **213/236 轮恰好 = 15（90.3%）**，规模由 `cap=15` 决定而非信号决定。「97% 落在 [5,15]」是 cap 的算术副产品，**不能作为形态门工作良好的证据**。真实候选量 `n_hot_active` 收盘 **37**，是队列容量的 **2.5 倍** ⇒ **约 60% 合格标的被 cap 无痕丢弃**。
2. **涨幅集中度换成了「板块集中度」。** 41 只中 **元件 18 只 = 43.9%**，叠加塑料(3)/电子化学品(1)，**PCB-元件簇过半**。旧偏差是「只追涨」，新偏差是「只追元件」。**不是更分散，只是换了个方向集中。**
3. **队列极度粘滞，近乎静态。** 前 5 名占 229/226/226/214/212 轮，**56.6% 相邻轮次零变化**。全天 41 只成员，但任一时刻实质只有 5–6 只「主力」。更像「T-1 战法池的一次性快照」，而非「盘中动态确认」。
4. **全天 0 触发 / 0 成交。** `triggered` 全程为空，执行链路一次都没被激活。既非静默丢弃、也非拒绝——**是队列与触发条件完全不咬合**。需用户判断这属「首样本正常」还是「触发条件与形态门输出不兼容」。
5. **计划票只放行 1/4，且无法归因。** 4 只全在池内（可见），但队列只放行 002815（25%）。**今日无 `funnel.rejects` 落痕 ⇒ 逐只归因不可能**。该字段已由 `e59b0a1` 补上，**明日即可定论**。
6. **与选手对照仅 50%。** 4 只候选命中 2 只；**选手实际买入的国脉科技(002093) 全天未进队列** ⇒ 漏掉选手一半实际执行票。
7. **`strong_mainline.calibrated = false`。** 强势主线闸门运行在**未标定**状态，却已把峰值 **22 只**以「strong_mainline_only」身份放进队列。
8. **🔴 R4 修复的生效前提未就位。** `e59b0a1`(15:22) 的成交额改口径依赖池内 `prev_amt_yi`，而**今日池 1246 条记录 0 条含该字段**。若不重建池，明日仍会退回当日累计口径。

### 用户决策点的直给判断

**「队列规模」不应作为这次改造的验收指标**——它已饱和在 cap 上，无区分度。
**真正该看的是三个数**：① `n_hot_active`（真实候选量）② 板块集中度 ③ 全天触发数。
以 9/15 计：**37 / 元件 43.9% / 0 触发**。

### 下一步建议（供用户裁定，本任务不执行）

| 优先级 | 建议 | 依据 |
|---|---|---|
| **P0** | **重建战法池**（`plan_daily.py`）使 `prev_amt_yi` 就位，否则 `e59b0a1` 的 R4 修复不生效 | §0 第 2 条：1246 条记录 0 条含该字段 |
| **P0** | 明日回收 `funnel.rejects`，对 4 只计划票逐只归因 | §8：今日无落痕，只能推断 |
| **P1** | 给 **`cap=15` 截断加留痕**（记录被丢弃的 sym 列表） | 收盘 22 只（59.5%）无痕丢弃 |
| **P1** | 加 **L2 板块配额**（如单 L2 ≤ 30%），破除元件单极 | 元件 43.9% |
| **P2** | 复核 **触发条件与形态门输出的兼容性**（0 触发根因） | §4：全天 0 triggered |
| **P2** | 对 `strong_mainline` 做**标定**或暂时降权 | `calibrated=false` 却影响峰值 22 只 |
| **P2** | 复核计划 `sig_date` 与池 `sig_date` 不一致（4 中 3） | §8.1 |

---

## 附录 A：逐轮漏斗时间线

| 时段 | n_pat_active | n_hot_active | n_queue_capped | len(movers) |
|---|---|---|---|---|
| 09:30–09:32 | 0（预热） | 0 | 0 | 0 |
| 09:33–09:37 | 1 | 1 | 1 | 1 |
| 09:38–09:59 | 3 → 18 | 3 → 17 | 3 → 15 | 3 → 20 |
| 10:00–11:30 | 18 → 31 | 17 → 27 | 15（恒满） | 20（恒满） |
| 13:00–14:00 | 31 → 40 | 27 → 33 | 15（恒满） | 20（恒满） |
| 14:00–15:00 | 40 → 45 | 33 → 37 | 15（恒满） | 20（恒满） |

**复现命令**（项目根 = `C:\Users\YZP\WorkBuddy\Claw\方法论与研究文档\EvoAlpha\yaoban-system`）：
```bash
python -c "import glob,json,os;d='outputs/intraday';[print(os.path.basename(f),len(json.load(open(f,encoding='utf-8'))['movers']),len(json.load(open(f,encoding='utf-8'))['confirm_queue']),json.load(open(f,encoding='utf-8'))['pattern_meta']['funnel']) for f in sorted(glob.glob(os.path.join(d,'confirm_20260915_*.json')))]"
```

## 附录 B：数据来源清单（全部只读）

| 用途 | 路径 |
|---|---|
| 队列/漏斗/触发 | `yaoban-system\outputs\intraday\confirm_20260915_*.json`（236 个） |
| 战法池成员与 `asof` | `yaoban-system\outputs\patterns\2026-09-15_pattern_pool.json`（`pool` 1246 条，`built_at` 2026-09-15 06:03:14） |
| scan rc 分布 | `yaoban-system\outputs\task_logs\2026-09-15\*_scan.json`（326 个）+ 同名 `.stdout.log` / `.stderr.log` |
| 盘前计划 | `yaoban-system\outputs\plans\2026-09-15_plan.json` |
| 选股链源码（只读参照） | `yaoban-system\scripts\scan_and_confirm.py:355-538` |
| 选手候选池 | `EvoAlpha\选手学习资料\文字资料\2026-09-15_0945_候选池\record.md` |
| 选手实际执行 | `EvoAlpha\选手学习资料\文字资料\2026-09-15_1048_持仓截图\record.md` |
| 既有 RCA | `EvoAlpha\docs\RCA_PLAN_PICKS_MISS_20260915.md` |
| 代码变更时间戳 | `git log`（`yaoban-system` @ `e59b0a1`，2026-09-15 15:22） |
| 进程心跳（旁证） | `outputs\intraday\_tick_daemon.beat`（15:23:13, pid 40952, interval 5）、`_tick_watch.beat`（15:23:01, restarts=0, degraded=0） |

**本次采集未修改任何代码、未调整任何参数、未写入任何 outputs 目录。**
