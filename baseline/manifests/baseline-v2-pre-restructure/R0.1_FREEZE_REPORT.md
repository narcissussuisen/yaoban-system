# R0.1 baseline-v2 冻结报告

> 冻结时间：2026-09-12 11:58:50 (Asia/Shanghai)
> baseline_id：`baseline-v2-pre-restructure`
> 依据：`EvoAlpha/docs/EVOALPHA_V2_RESTRUCTURE_PLAN.md` §5 R0.1；产出工具 `tools/make_baseline_manifest.py`
> 用途：v2.0 重构前的**生产真实状态存证** + `decision_digest` 重放所需的**冻结基线窗口**

## 1. 冻结结果

| 项 | 值 |
|---|---|
| 文件数 | **2,403** |
| 总字节 | **190,033,439**（约 181 MB） |
| **manifest_hash (SHA-256)** | `6ee6057e0ec1df19cd91b52a88735d2d9504a0f032de23cddf289bcb7abaad6f` |
| 产物目录 | `yaoban-system/baseline/manifests/baseline-v2-pre-restructure/` |
| 产物 | `manifest.jsonl`（逐文件 path/size/mtime/sha256）、`summary.json`、`tasks_registered.txt` |

## 2. 冻结范围分布

| 范围 | 文件数 | 字节 | 说明 |
|---|---|---|---|
| data | 1,951 | 187,201,639 | `yaoban.db`、`journal.db`、`minute/`、行业与股票名映射 |
| docs | 124 | 710,297 | 全量：`INCIDENT_LOG.md`、`reviews/`、`loops/state.json` 与阶段结论 |
| scripts | 223 | 1,196,670 | 全部生产/研究脚本与 `*.ps1`/`*.cmd`/`*.xml` 任务定义 |
| src | 39 | 309,209 | 核心库（含 `iteration/` 12 模块） |
| tests | 39 | 214,469 | 测试 |
| **tools** | **7** | **83,543** | `run_intraday_counterfactual.py`、`snapshot_minute.py`、`make_baseline_manifest.py` 等 |
| portfolio | 5 | 62,506 | `ledger.json` 权威账本 + `ledger.py` + preflight 备份账本 |
| config | 3 | 18,329 | `parameters.toml`、`universe.toml`、`concepts.json` |
| templates | 1 | 9,850 | 模板 |
| 根目录 | 11 | 224,613 | `_r7p_review_*.py` 探针 ×9、`README.md`、`vendor_astock_skill.md` |

**排除规则**：`__pycache__`/`.git`/`.mypy_cache`/`.pytest_cache`、`tmp*` 临时目录、`*.pyc/*.pyo/*.log/*.tmp/*.lock`、`ledger.lock`。

### 2.1 本次对冻结工具的两处修正（相对 baseline-0）

| # | 问题 | 修正 |
|---|---|---|
| 1 | `tools/` **未纳入**冻结范围 —— 而它含 `run_intraday_counterfactual.py`（9/11 决定性证伪的复现脚本）与 `snapshot_minute.py` | `SCOPES` 增补 `"tools"`（+7 文件） |
| 2 | `docs/` **只冻结 `docs/loops`** —— 而 `INCIDENT_LOG.md`（接手故障必读权威）与 `reviews/` 未存证 | `SCOPES` 由 `docs/loops` 扩为 `docs`（28 → 124 文件） |
| 3 | `TASK_NAMES` 只列 4 个（`YaobanDailyCandidates` 等，**其中 3 个实为 Disabled 的旧任务**） | 与 `scripts/register_schedule.ps1::$Schedule` 单一真相源对齐为 **19 项** |
| 4 | `schtasks` 输出按 `encoding="utf-8"` 解码 → zh-CN 控制台为 GBK，任务明细全变乱码；且过滤键只有英文标签，在中文系统上**一条也匹配不到**（baseline-0 的 `tasks_registered.txt` 实际只有状态行） | 增 `_decode_console()`（utf-8→gbk→cp936 回退）+ `_TASK_KEEP_KEYS` 兼容中英标签；现可读到「下次运行时间 / 上次运行时间 / 上次结果 / 要运行的任务」 |

## 3. 代码来源存证（git）

| 项 | 值 |
|---|---|
| HEAD commit | `d6a70aa0fe5421d685a4378b9900f00a020a56b5` |
| 分支 | `main` |
| **未提交改动** | **45 个文件** ⚠️ |

> ⚠️ 冻结时存在 45 个未提交改动（重构前遗留的在途工作）。manifest 逐文件 sha256 已全部存证，故**内容可复现**；但引用本 baseline 时必须同时引用 commit + manifest_hash，不可只引 commit。

## 4. 冻结基线窗口（decision_digest 重放依据）

```json
"ledger_window": {
  "start": "2026-08-31", "end": "2026-09-11", "points": 9,
  "start_cash": 100000.0, "start_date": "2026-08-31",
  "fills": 8, "positions": 0, "revision": 36,
  "risk_state": { "position_multiplier": 0.0, "paused": true, "terminated": false,
                  "peak_equity": 101917.58, "drawdown_pct": -10.64,
                  "source_date": "2026-09-11", "reason": "portfolio_drawdown_pause" }
}
```

| 项 | 值 | 含义 |
|---|---|---|
| 净值序列窗口 | **2026-08-31 → 2026-09-11**（9 个交易日点） | `decision_digest` 重放的冻结基线窗口 |
| fills | 8 笔 | 决策集重放的对照真值 |
| positions | 0 | 冻结时已空仓 |
| ⚠️ risk_state | **`paused=true`、`drawdown_pct=-10.64`、`position_multiplier=0.0`** | 熔断已于 9/11 触发（`portfolio_drawdown_pause`）→ **R0.2 新账本必须清零这三项**，否则 `ledger._validate_buy_policy` 直接禁新仓 |
| ⚠️ start_cash | 100,000 | R0.2 将改为 **500,000**（8.1 裁决） |

> **日线/分钟数据窗口**未记录在本报告：数据不在本 repo 内（分钟数据经 `QFQStore` 读取外部路径），窗口登记归 R2 数据层。

## 5. 计划任务注册状态（存证时点）

**19 / 19 全部 REGISTERED**（与 `register_schedule.ps1` 声明表一致）：

`YaobanPreflight` · `YaobanSelfHeal` · `YaobanPremarket` · `YaobanPlanGate` · `YaobanMorningCheck` · `YaobanTdxProbe` · `YaobanAuctionMonitor` · `YaobanEventNotify` · `YaobanTickDaemon` · `YaobanScanConfirm` · `YaobanIntradayMonitor` · `YaobanClosePipeline` · `YaobanPostCloseChain` · `YaobanEveningCheck` · `YaobanTdxServerVerify` · `YaobanBoardRefresh` · `YaobanStatusPush` · `VibeResearchDashboardServices` · `VibeResearchLiveTickValidation`

明细（下次运行时间均为 **2026/9/14 周一**，上次运行 **2026/9/11**，上次结果 **0**）见 `tasks_registered.txt`。

## 6. 验收状态

- [x] 存证 manifest 生成，顶层哈希可引用
- [x] 冻结窗口（净值序列 + fills）记录，可供 `decision_digest` 重放
- [x] 任务注册状态与声明表逐项核对（19/19）
- [x] 冻结范围自洽性检查：`tools/` 与 `docs/` 补齐
- [ ] 「同一输入可重放同一决策集」实跑验证 —— 由 R5.0（分层晋级机制）的 `invariance_replay()` 首次执行时闭合

## 7. 语义声明

本 manifest 冻结的是 **v2.0 重构启动前的生产真实状态**（含 §4 所示的 9/11 熔断态与 45 个未提交改动），
仅作对照存证与重放基线。**不得**作为收益或学习结论的基准——收益判定一律走 R5.1 净值判定器（vs 双市场基准臂）。
